[module/audio]
type = internal/pulseaudio

format-volume-foreground = ${colors.foreground-audio}
format-muted-foreground = ${colors.foreground-audio}

format-volume =   <label-volume>

reverse-scroll = false
use-ui-max = false

label-muted =  0%

interval = 5
